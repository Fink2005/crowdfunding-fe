services:
  crowdfunding-fe:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: crowdfunding-fe
    ports:
      - '8386:8386'
    environment:
      - .env
    restart: unless-stopped
    networks:
      - crowdfunding-network

networks:
  crowdfunding-network:
    driver: bridge
